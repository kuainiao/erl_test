%%%-------------------------------------------------------------------
%%% @author yujian
%%% @doc
%%% Created : 20. 二月 2016 下午2:44
%%%-------------------------------------------------------------------
-module(leofs_magick).

%%-on_load(init/0).
%%-export([convert/1]).
%%
%%init() ->
%%    Path = case code:lib_dir(test, priv) of
%%               {error, _} -> "./priv/leofs_magick";
%%               Str -> Str ++ "/leofs_magick"
%%           end,
%%
%%    erlang:load_nif(Path, 0).
%%
%%convert(_Bin) ->
%%    erlang:error({"NIF not implemented in leofs_magick at line", ?LINE}).
